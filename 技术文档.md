# 智瞳语音辅助应用 - 技术文档

---

## 一、项目概述

### 1.1 项目定位

**智瞳**是一款面向视障人士的语音辅助应用，基于 **Capacitor + Vue 3** 构建的跨平台应用，核心功能是**端侧离线语音交互**，无需网络即可完成语音识别和语音播报。

### 1.2 核心特性

| 特性 | 说明 |
|---|---|
| **离线语音识别** | 基于 ONNX 模型，无需网络连接 |
| **按住说话模式** | Push-to-Talk，只有按住麦克风才录音 |
| **实时语音播报** | 基于 Android 系统 TTS 引擎 |
| **命令意图识别** | 支持导航、音量调节、帮助查询等指令 |
| **多页面支持** | 用户主页、监护人页、相机页、紧急求助页等 |

### 1.3 应用场景

- **视障人士日常使用**：通过语音指令进行页面导航、功能操作
- **紧急求助**：一键触发紧急呼叫、发送位置信息
- **物体识别**：结合相机进行实时物体识别并语音播报结果

---

## 二、技术架构

### 2.1 架构分层

整个应用采用**三层架构**设计，层次清晰，职责明确：

| 层级 | 名称 | 技术栈 | 职责 |
|---|---|---|---|
| **第一层** | 前端展示层 | Vue 3 + Vue Router | UI 展示、页面路由、用户交互 |
| **第二层** | 业务逻辑层 | JavaScript | 语音交互服务、命令处理、状态管理 |
| **第三层** | 原生能力层 | Kotlin + Java | 语音识别引擎、语音合成、系统能力 |

### 2.2 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         第一层：前端展示层                                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐    │
│  │ 页面组件    │  │ 页面组件    │  │ 页面组件    │  │ 麦克风按钮组件   │    │
│  │ (9个页面)   │  │ (views/)    │  │ (views/)    │  │MicrophoneButton │    │
│  └─────┬───────┘  └─────┬───────┘  └─────┬───────┘  └────────┬────────┘    │
│        │                │                │                    │             │
│        └────────────────┼────────────────┼────────────────────┘             │
│                         ▼                                                 │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Vue Router                                  │   │
│  │              路由管理、页面切换、路由守卫                            │   │
│  └────────────────────────────┬───────────────────────────────────────┘   │
└───────────────────────────────┼───────────────────────────────────────────┘
                                │
                                ▼ Capacitor Bridge (JS Native Bridge)
                                │
┌───────────────────────────────┼───────────────────────────────────────────┐
│                         第二层：业务逻辑层                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    VoiceInteractionService                          │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐    │   │
│  │  │ TTS播报  │  │ ASR识别  │  │ 命令处理  │  │ 状态管理         │    │   │
│  │  │ speak()  │  │ listen() │  │ handle() │  │ isSpeaking等     │    │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────┬───────────────────────────────────────────┘
                                │
                                ▼ Capacitor Plugin Call
                                │
┌───────────────────────────────┼───────────────────────────────────────────┐
│                         第三层：原生能力层                                   │
│  ┌─────────────────────┐                    ┌─────────────────────┐       │
│  │    NativeASRPlugin  │                    │    NativeTTSPlugin  │       │
│  │  (语音识别插件)      │                    │    (语音播报插件)    │       │
│  └──────────┬──────────┘                    └──────────┬──────────┘       │
│             │                                          │                  │
│             ▼                                          ▼                  │
│  ┌─────────────────────┐                    ┌─────────────────────┐       │
│  │      AsrEngine.kt   │                    │   Android TTS API   │       │
│  │  (ONNX推理引擎)      │                    │   (系统语音合成)     │       │
│  └──────────┬──────────┘                    └─────────────────────┘       │
│             │                                                              │
│             ▼                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      模型与资源文件                                 │   │
│  │  model_quant.onnx  │  tokens.json  │  am.mvn  │  FbankExtractor  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────────────────┘
```

### 2.3 技术栈详解

| 分类 | 技术 | 版本 | 说明 |
|---|---|---|---|
| **前端框架** | Vue | 3.5.13 | 响应式 UI 框架，Composition API |
| **路由管理** | Vue Router | 4.6.4 | 单页应用路由管理 |
| **跨平台桥接** | Capacitor | 8.3.4 | Web 与 Native 之间的桥接层 |
| **构建工具** | Vite | 6.3.5 | 快速构建工具，支持热更新 |
| **语音识别** | ONNX Runtime | 1.16.3 | 端侧深度学习模型推理 |
| **语音合成** | Android TTS | 系统内置 | 原生语音播报引擎 |
| **前端语言** | JavaScript | ES6+ | 前端业务逻辑开发 |
| **原生语言** | Kotlin | 1.9.22 | 语音引擎核心算法开发 |
| **原生语言** | Java | 17 | Android 插件开发 |

---

## 三、目录结构

### 3.1 项目完整目录结构

```
zhitong-main/                          # 项目根目录
├── .gradle/                           # Gradle 缓存目录
├── .vscode/                           # VS Code 配置
├── android/                           # Android 原生项目（主应用）
│   ├── app/                           # Android 应用模块
│   │   ├── src/                       # 源码目录
│   │   │   ├── androidTest/           # 仪器化测试
│   │   │   ├── main/                  # 主源码
│   │   │   │   ├── assets/            # 资源文件（模型、配置）
│   │   │   │   │   ├── am.mvn         # CMVN 归一化参数
│   │   │   │   │   ├── model_quant.onnx  # ONNX 量化模型
│   │   │   │   │   └── tokens.json    # 字符映射表
│   │   │   │   ├── java/              # Java 源码
│   │   │   │   │   └── com/
│   │   │   │   │       └── sightsmart/
│   │   │   │   │           └── visionassist/
│   │   │   │   │               ├── plugins/  # Capacitor 插件
│   │   │   │   │               │   ├── NativeASRPlugin.java
│   │   │   │   │               │   └── NativeTTSPlugin.java
│   │   │   │   │               ├── MainActivity.java  # Android 入口
│   │   │   │   │               ├── AsrDecoder.kt     # 解码器
│   │   │   │   │               ├── AsrEngine.kt      # 推理引擎
│   │   │   │   │               └── FbankExtractor.kt # 特征提取
│   │   │   │   ├── res/               # 资源文件（图片、布局等）
│   │   │   │   └── AndroidManifest.xml # 应用配置
│   │   │   └── test/                  # 单元测试
│   │   ├── .gitignore
│   │   ├── build.gradle               # 模块构建配置
│   │   ├── capacitor.build.gradle     # Capacitor 构建配置
│   │   └── proguard-rules.pro         # 混淆规则
│   ├── gradle/                        # Gradle 包装器
│   ├── build.gradle                   # 项目构建配置
│   ├── settings.gradle                # 项目设置
│   └── gradle.properties              # Gradle 属性
├── android_app/                       # 纯 Android 演示项目（独立）
├── app/                               # 备用应用模块
├── falldowncheck/                     # 跌倒检测模块
├── docs/                              # 文档目录
├── public/                            # 静态资源（Web）
├── src/                               # 前端源码（Vue）
│   ├── assets/                        # 静态资源
│   ├── capacitor/                     # Capacitor 插件桥接
│   │   └── plugins/
│   │       ├── NativeASR/             # ASR 插件 JS 接口
│   │       │   └── NativeASR.js
│   │       └── NativeTTS/             # TTS 插件 JS 接口
│   │           └── NativeTTS.js
│   ├── components/                    # 可复用组件
│   │   ├── HelloWorld.vue             # 示例组件
│   │   └── MicrophoneButton.vue       # 全局麦克风按钮
│   ├── router/                        # 路由配置
│   │   └── index.js                   # 路由定义
│   ├── utils/                         # 工具服务
│   │   └── voiceInteraction.js        # 语音交互核心服务
│   ├── views/                         # 页面组件
│   │   ├── camerapage.vue             # 相机拍摄页
│   │   ├── emergencepage.vue          # 紧急处理页
│   │   ├── guardian01.vue             # 监护人主页
│   │   ├── guardian_personal.vue      # 监护人个人中心
│   │   ├── initializepage.vue         # 启动页
│   │   ├── selectmodle.vue            # 模式选择页
│   │   ├── sospage.vue                # 紧急求助页
│   │   ├── user01.vue                 # 用户主页
│   │   ├── userpersonal.vue           # 用户个人中心
│   │   └── voicedebug.vue             # 语音测试页
│   ├── views1/                        # 静态 HTML 页面备份
│   ├── App.vue                        # 根组件
│   ├── main.js                        # 应用入口
│   └── style.css                      # 全局样式
├── .gitignore
├── README.md
├── capacitor.config.json              # Capacitor 配置
├── index.html                         # HTML 入口
├── package.json                       # 前端依赖配置
└── vite.config.js                     # Vite 配置
```

---

## 四、核心文件详解

### 4.1 前端核心文件

#### 4.1.1 应用入口与根组件

| 文件 | 路径 | 功能说明 | 核心职责 |
|---|---|---|---|
| **main.js** | `src/main.js` | 应用入口 | 创建 Vue 实例、注册路由 |
| **App.vue** | `src/App.vue` | 根组件 | 初始化语音服务、路由监听、全局组件挂载 |

**App.vue 关键逻辑**：
- `onMounted`: 初始化 `voiceInteraction` 服务
- `watch(route)`: 路由切换时播报页面信息
- 挂载全局麦克风按钮组件

#### 4.1.2 路由配置

| 文件 | 路径 | 功能说明 |
|---|---|---|
| **router/index.js** | `src/router/index.js` | 定义 10 个页面路由，包含路由守卫 |

**路由清单**：

| 路由路径 | 组件 | 名称 |
|---|---|---|
| `/` | `InitializePage` | 启动页 |
| `/selectmodle` | `SelectModel` | 模式选择页 |
| `/user01` | `User01` | 用户主页 |
| `/guardian01` | `Guardian01` | 监护人主页 |
| `/camerapage` | `CameraPage` | 相机拍摄页 |
| `/userpersonal` | `UserPersonal` | 用户个人中心 |
| `/guardian_personal` | `GuardianPersonal` | 监护人个人中心 |
| `/emergencepage` | `EmergencePage` | 紧急处理页 |
| `/sospage` | `SosPage` | 紧急求助页 |
| `/voicedebug` | `VoiceDebug` | 语音测试页 |

#### 4.1.3 语音交互服务（核心）

| 文件 | 路径 | 功能说明 |
|---|---|---|
| **voiceInteraction.js** | `src/utils/voiceInteraction.js` | 语音交互核心服务，单例模式 |

**核心方法**：

| 方法名 | 功能 | 参数 | 返回值 |
|---|---|---|---|
| `init()` | 初始化服务，加载模型 | `onCommand, onNavigate` | `{ttsAvailable, sttAvailable, modelLoaded}` |
| `speak()` | 语音播报 | `text, lang` | `void` |
| `stop()` | 停止播报 | 无 | `void` |
| `startPushToTalk()` | 开始按住说话 | 无 | `void` |
| `stopPushToTalk()` | 停止按住说话，获取识别结果 | 无 | `string|null` |
| `handleCommand()` | 命令意图识别与处理 | `transcript` | `void` |
| `announcePage()` | 播报页面信息 | `pagePath, extraInfo` | `void` |
| `loadModel()` | 加载语音识别模型 | 无 | `boolean` |

**状态管理**：

| 状态字段 | 类型 | 说明 |
|---|---|---|
| `isSpeaking` | boolean | 是否正在播报 |
| `isListening` | boolean | 是否正在录音 |
| `isModelLoaded` | boolean | 模型是否加载完成 |
| `nativeASRAvailable` | boolean | ASR 插件是否可用 |
| `nativeTTSAvailable` | boolean | TTS 插件是否可用 |
| `currentPage` | string | 当前页面路径 |
| `volume` | number | 音量（0-1） |
| `rate` | number | 语速（0.5-2.0） |

#### 4.1.4 Capacitor 插件桥接

| 文件 | 路径 | 功能说明 |
|---|---|---|
| **NativeASR.js** | `src/capacitor/plugins/NativeASR/NativeASR.js` | ASR 插件 JS 桥接，定义 Web 端 fallback |
| **NativeTTS.js** | `src/capacitor/plugins/NativeTTS/NativeTTS.js` | TTS 插件 JS 桥接，定义 Web 端 fallback |

**NativeASR 接口**：

| 方法 | 功能 |
|---|---|
| `initialize()` | 初始化语音识别模型 |
| `isInitialized()` | 检查模型是否已初始化 |
| `startListening()` | 开始录音 |
| `stopListening()` | 停止录音，返回识别结果 |

#### 4.1.5 页面组件

| 文件 | 路径 | 功能说明 |
|---|---|---|
| **initializepage.vue** | `src/views/initializepage.vue` | 启动页，引导用户点击开始，加载模型 |
| **selectmodle.vue** | `src/views/selectmodle.vue` | 模式选择页，选择用户/监护人模式 |
| **user01.vue** | `src/views/user01.vue` | 用户主页，显示识别历史、功能入口 |
| **guardian01.vue** | `src/views/guardian01.vue` | 监护人主页，查看被监护人状态 |
| **camerapage.vue** | `src/views/camerapage.vue` | 相机拍摄页，拍照识别物体 |
| **userpersonal.vue** | `src/views/userpersonal.vue` | 用户个人中心，管理个人资料和设置 |
| **guardian_personal.vue** | `src/views/guardian_personal.vue` | 监护人个人中心 |
| **emergencepage.vue** | `src/views/emergencepage.vue` | 紧急处理页，处理紧急情况 |
| **sospage.vue** | `src/views/sospage.vue` | 紧急求助页，发起求救 |
| **voicedebug.vue** | `src/views/voicedebug.vue` | 语音测试页，调试语音功能 |

#### 4.1.6 通用组件

| 文件 | 路径 | 功能说明 |
|---|---|---|
| **MicrophoneButton.vue** | `src/components/MicrophoneButton.vue` | 全局麦克风按钮，按住说话模式 |

**交互逻辑**：
- `@mousedown/@touchstart`: 触发 `startPushToTalk()`
- `@mouseup/@touchend/@mouseleave`: 触发 `stopPushToTalk()`
- 按住时显示红色高亮和波纹动画

### 4.2 Android 原生核心文件

#### 4.2.1 入口与插件注册

| 文件 | 路径 | 功能说明 |
|---|---|---|
| **MainActivity.java** | `android/app/src/main/java/.../MainActivity.java` | Android 应用入口，注册 Capacitor 插件 |

**插件注册**：
- NativeASRPlugin
- NativeTTSPlugin

#### 4.2.2 Capacitor 插件实现

| 文件 | 路径 | 功能说明 |
|---|---|---|
| **NativeASRPlugin.java** | `android/.../plugins/NativeASRPlugin.java` | ASR 插件实现，管理录音和识别流程 |
| **NativeTTSPlugin.java** | `android/.../plugins/NativeTTSPlugin.java` | TTS 插件实现，调用系统语音合成 |

**NativeASRPlugin 核心方法**：

| 方法 | 功能 |
|---|---|
| `initialize()` | 初始化 ONNX 模型 |
| `isInitialized()` | 检查模型初始化状态 |
| `startListening()` | 启动录音线程，累积音频数据 |
| `stopListening()` | 停止录音，执行识别并返回结果 |

**NativeTTSPlugin 核心方法**：

| 方法 | 功能 |
|---|---|
| `speak()` | 调用 Android TextToSpeech 进行语音合成 |
| `stop()` | 停止当前播报 |
| `isSupported()` | 检查 TTS 是否可用 |

#### 4.2.3 语音识别引擎

| 文件 | 路径 | 功能说明 |
|---|---|---|
| **AsrEngine.kt** | `android/.../AsrEngine.kt` | ONNX 模型推理引擎，核心识别逻辑 |
| **AsrDecoder.kt** | `android/.../AsrDecoder.kt` | 解码器，将 token 序列转为中文文本 |
| **FbankExtractor.kt** | `android/.../FbankExtractor.kt` | 音频特征提取，提取 Fbank 特征 |

**AsrEngine 识别流程**：
1. 接收原始音频数据（short[]）
2. 调用 FbankExtractor 提取特征
3. 应用 CMVN 归一化（使用 am.mvn）
4. 调用 ONNX Runtime 执行推理
5. 调用 AsrDecoder 解码为中文文本

#### 4.2.4 模型与资源文件

| 文件 | 路径 | 功能说明 |
|---|---|---|
| **model_quant.onnx** | `android/.../assets/model_quant.onnx` | ONNX 量化语音识别模型 |
| **tokens.json** | `android/.../assets/tokens.json` | 字符映射表，token 到汉字的映射 |
| **am.mvn** | `android/.../assets/am.mvn` | CMVN（倒谱均值方差归一化）参数 |

---

## 五、核心功能实现

### 5.1 语音交互闭环流程

```
用户操作                    前端层 (JS)                    原生层 (Kotlin/Java)
─────────────────────────────────────────────────────────────────────────────
按住麦克风按钮
        │
        ▼
┌───────────────────────┐
│ startPushToTalk()     │
│  ├─ 停止当前播报       │
│  └─ NativeASR.startListening()
└──────────┬────────────┘
           │
           ▼ Capacitor Bridge
┌─────────────────────────────────────────────────────┐
│ NativeASRPlugin.startListening()                    │
│  ├─ 初始化 AudioRecord                              │
│  ├─ 创建录音线程                                    │
│  └─ 累积音频数据到 ArrayList<Short>                 │
└────────────────────────────┬────────────────────────┘
                            │
用户松开麦克风按钮           │
        │                   │
        ▼                   ▼
┌───────────────────────┐  ┌─────────────────────────────────────────────┐
│ stopPushToTalk()      │  │ NativeASRPlugin.stopListening()             │
│  ├─ 设置 isListening=false│  ├─ 停止录音线程                           │
│  └─ NativeASR.stopListening()│  ├─ 获取累积的音频数据                   │
└──────────┬────────────┘  │  ├─ AsrEngine.recognize(audioData)         │
           │               │  │     ├─ FbankExtractor.extract()         │
           │               │  │     ├─ ONNX Runtime 推理                 │
           │               │  │     └─ AsrDecoder.decode()              │
           │               │  └─ 返回 {success: true, text: "识别结果"}  │
           │               └────────────────────────────┬────────────────┘
           │                                          │
           │                                          ▼ Capacitor Bridge
           ▼
┌───────────────────────┐
│ handleCommand(text)   │
│  ├─ 命令意图识别       │
│  ├─ 执行对应操作       │
│  └─ speak() 播报反馈   │
└──────────┬────────────┘
           │
           ▼
┌───────────────────────┐
│ NativeTTS.speak()     │
└──────────┬────────────┘
           │
           ▼ Capacitor Bridge
┌─────────────────────────────────────────────────────┐
│ NativeTTSPlugin.speak()                             │
│  └─ Android TextToSpeech.speak()                   │
└─────────────────────────────────────────────────────┘
```

### 5.2 语音识别工作机制

#### 5.2.1 前端层流程

```
voiceInteraction.startPushToTalk()
        │
        ├─ 停止播报 (NativeTTS.stop())
        │
        └─ 启动监听 (NativeASR.startListening())
                │
                ▼
voiceInteraction.stopPushToTalk()
        │
        ├─ 获取识别结果 (NativeASR.stopListening())
        │
        └─ 处理命令 (handleCommand(text))
                │
                ├─ 查询页面命令 → speak("当前页面是...")
                ├─ 导航命令 → speak("正在进入...") + router.push()
                ├─ 帮助命令 → speak("可用语音指令包括...")
                ├─ 音量命令 → speak("音量已增大...")
                └─ 亮度命令 → speak("亮度调节需要在系统设置中操作...")
```

#### 5.2.2 原生层流程

```
NativeASRPlugin.startListening()
        │
        ├─ 检查麦克风权限
        │
        ├─ 初始化 AudioRecord (采样率 16000Hz)
        │
        └─ 启动录音线程
                │
                ├─ 循环读取麦克风数据
                │
                └─ 累积到 ArrayList<Short>

NativeASRPlugin.stopListening()
        │
        ├─ 停止录音线程
        │
        ├─ 释放 AudioRecord
        │
        └─ AsrEngine.recognize(audioData)
                │
                ├─ FbankExtractor.extract(audioData) → Fbank 特征
                │
                ├─ 应用 CMVN 归一化
                │
                ├─ ONNX Runtime.run(model, features) → token 序列
                │
                └─ AsrDecoder.decode(tokens) → 中文文本
```

### 5.3 命令意图识别

#### 5.3.1 命令类型与关键词

| 命令类型 | 触发关键词 | 匹配方法 |
|---|---|---|
| **查询页面** | 现在是什么页面、当前页面、我在哪里、这是什么页面、当前在哪个页面 | `isQueryPageCommand()` |
| **导航** | 进入、打开、去、跳转到、导航到、前往 | `isNavigationCommand()` |
| **帮助** | 帮助、指令、怎么说、有什么命令 | `isHelpCommand()` |
| **停止** | 关闭语音、停止语音、关闭助手 | `isStopCommand()` |
| **音量调节** | 音量、声音、大声、小声 | `isVolumeCommand()` |
| **亮度调节** | 亮度、屏幕亮度、屏幕、亮一点、暗一点 | `isBrightnessCommand()` |

#### 5.3.2 导航路由映射

```javascript
const routeMap = {
  '起始页': '/',
  '首页': '/user01',
  '主页': '/user01',
  '用户主页': '/user01',
  '模式选择': '/selectmodle',
  '选择模式': '/selectmodle',
  '相机': '/camerapage',
  '拍摄': '/camerapage',
  '拍照': '/camerapage',
  '个人中心': '/userpersonal',
  '个人': '/userpersonal',
  '用户个人': '/userpersonal',
  '紧急': '/emergencepage',
  '紧急处理': '/emergencepage',
  '求助': '/emergencepage',
  '监护人': '/guardian01',
  '监护人主页': '/guardian01',
  '监护人个人': '/guardian_personal',
  '求救': '/sospage',
  '紧急求助': '/sospage'
}
```

### 5.4 语音播报工作机制

**前端层**：
```javascript
async speak(text, lang = 'zh-CN') {
  await NativeTTS.speak({
    text: text,
    lang: lang,
    rate: this.rate,
    pitch: this.pitch
  })
}
```

**原生层**：
```java
@PluginMethod
public void speak(PluginCall call) {
  String text = call.getString("text");
  String lang = call.getString("lang", "zh-CN");
  float rate = call.getFloat("rate", 1.0f);
  
  HashMap<String, String> params = new HashMap<>();
  params.put(TextToSpeech.Engine.KEY_PARAM_LANGUAGE, lang);
  params.put(TextToSpeech.Engine.KEY_PARAM_RATE, String.valueOf(rate));
  
  textToSpeech.speak(text, TextToSpeech.QUEUE_FLUSH, params, null);
  call.resolve(new JSObject().put("success", true));
}
```

---

## 六、数据流转

### 6.1 语音识别数据流

```
麦克风输入
    │
    ▼ AudioRecord (16kHz采样)
short[] 原始音频
    │
    ▼ 累积到 ArrayList<Short>
ArrayList<Short> 完整录音
    │
    ▼ 转换为 short[]
short[] audioData
    │
    ▼ AsrEngine.recognize()
    │
    ├─ FbankExtractor.extract()
    │       │
    │       ▼
    │   float[][] fbankFeatures (特征矩阵)
    │       │
    │       ▼ 应用 CMVN
    │   float[][] normalizedFeatures
    │       │
    │       ▼
    │   ONNX Runtime.run()
    │       │
    │       ▼
    │   float[][] logits (模型输出)
    │       │
    │       ▼
    │   int[] tokens (argmax 获取 token)
    │       │
    │       ▼
    │   AsrDecoder.decode()
    │       │
    │       ▼
    │   String result (中文文本)
    │
    ▼
返回识别结果
    │
    ▼ Capacitor Bridge
    │
    ▼ JavaScript 层
    │
    ▼ handleCommand() 处理
```

### 6.2 页面导航数据流

```
语音命令 → handleCommand()
        │
        ├─ isNavigationCommand() 判断
        │
        ├─ extractNavigationRoute() 提取路由
        │       │
        │       ▼
        │   路由路径 (如 '/camerapage')
        │
        ├─ handleNavigation(route)
        │       │
        │       ├─ speak("正在进入相机拍摄页")
        │       │
        │       └─ onNavigate(route)
        │               │
        │               ▼
        │           router.push(route)
        │               │
        │               ▼
        │           Vue Router 页面切换
        │               │
        │               ▼
        │           App.vue watch 触发
        │               │
        │               ▼
        │           announcePageWithFeatures()
        │               │
        │               ▼
        │           speak("已进入相机拍摄页。主要功能：拍照识别...")
        │
        └─ onCommand({type: 'navigate', route, pageName})
```

---

## 七、关键设计特点

### 7.1 按住说话模式（Push-to-Talk）

**设计优势**：
- **隐私保护**：只有按住麦克风时才开启录音，保护用户隐私
- **避免误触发**：松开后才进行识别，避免环境噪音干扰
- **即时打断**：按住时立即停止当前播报，优先响应用户输入

**实现细节**：
- 使用 `mousedown/touchstart` 事件触发开始录音
- 使用 `mouseup/mouseleave/touchend/touchcancel` 事件触发停止录音
- UI 反馈：按住时按钮变红并显示波纹动画

### 7.2 端侧离线识别

**技术特点**：
- **无需网络**：ONNX 模型打包在 APK 中，完全离线运行
- **低延迟**：本地推理，无需等待网络传输
- **数据安全**：语音数据不上传云端，保护用户隐私

**模型部署**：
- 模型文件：`model_quant.onnx`（量化模型，减小体积）
- 配套文件：`tokens.json`（字符映射）、`am.mvn`（归一化参数）

### 7.3 状态管理机制

**核心状态**：
```javascript
{
  isSpeaking: false,      // 是否正在播报
  isListening: false,     // 是否正在录音
  isModelLoaded: false,   // 模型是否加载完成
  nativeASRAvailable: false,  // ASR 插件是否可用
  nativeTTSAvailable: false,  // TTS 插件是否可用
  currentPage: '',        // 当前页面路径
  volume: 1.0,            // 音量 (0-1)
  rate: 1.0,              // 语速 (0.5-2.0)
  pitch: 1.0              // 音调
}
```

### 7.4 防抖机制

```javascript
lastCommandTime: 0,           // 上次命令时间戳
commandDebounceMs: 1500,      // 防抖间隔 1.5 秒

// 在 handleCommand 中
const now = Date.now()
if (now - this.lastCommandTime < this.commandDebounceMs) {
  return  // 忽略本次命令
}
this.lastCommandTime = now
```

**作用**：避免短时间内重复识别相同命令，提升用户体验。

### 7.5 错误处理机制

**错误码映射**：
```javascript
errorMessages = {
  'network': '网络连接异常，请检查网络设置',
  'not-allowed': '麦克风权限被拒绝',
  'service-not-allowed': '语音服务不可用',
  'bad-grammar': '语音识别语法错误',
  'language-not-supported': '不支持的语言',
  'no-speech': '未检测到语音',
  'aborted': '语音识别已取消',
  'audio-capture': '无法访问麦克风设备',
  'unknown': '未知错误'
}
```

---

## 八、构建与部署

### 8.1 前端构建

```bash
# 安装依赖
npm install

# 构建生产版本
npm run build          # 输出到 dist/ 目录

# 同步到 Android
npx cap copy android   # 复制 dist/ 到 android/app/src/main/assets/public/
```

### 8.2 Android 构建

**前置条件**：
- Android Studio Hedgehog (2023.1.1) 或更高版本
- JDK 17
- Gradle 8.13

**构建步骤**：
1. 打开 Android Studio
2. 导入项目：打开 `android/` 目录
3. Sync Project with Gradle Files（首次 Sync 需要下载依赖）
4. 连接 Android 设备或启动模拟器
5. 点击 Run 按钮（绿色三角形）或 Build APK

### 8.3 依赖说明

| 依赖 | 版本 | 用途 |
|---|---|---|
| `@capacitor/android` | ^8.3.4 | Capacitor Android 平台支持 |
| `@capacitor/core` | ^8.3.4 | Capacitor 核心 API |
| `vue-router` | ^4.6.4 | Vue 路由管理 |
| `onnxruntime-android` | 1.16.3 | ONNX 模型推理（Android 端） |
| `com.google.gms:google-services` | 4.4.4 | Google 服务支持 |

### 8.4 Gradle 配置要点

**build.gradle（项目级别）**：
```groovy
buildscript {
    dependencies {
        classpath 'com.android.tools.build:gradle:8.13.0'
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.22'
    }
}
```

**build.gradle（应用级别）**：
```groovy
dependencies {
    implementation 'com.microsoft.onnxruntime:onnxruntime-android:1.16.3'
    implementation "org.jetbrains.kotlin:kotlin-stdlib:1.9.22"
}
```

---

## 九、总结

### 9.1 架构优势

1. **跨平台能力**：一套前端代码运行在 Web 和 Android 平台
2. **离线可用**：语音识别完全在端侧完成，无需网络
3. **隐私保护**：语音数据不上传云端，本地处理
4. **视障友好**：语音交互为主，麦克风按钮固定在右上角，方便定位
5. **可扩展性**：模块化设计，易于添加新功能和命令

### 9.2 功能闭环

```
应用启动 → 加载模型 → 等待用户交互
                │
                ▼
用户按住麦克风 → 停止播报 → 开始录音
                │
                ▼
用户松开麦克风 → 停止录音 → 执行识别 → 意图解析 → 语音反馈/页面导航
                │
                ▼
等待下一次交互
```

### 9.3 扩展能力

- **新增命令**：在 `handleCommand()` 中添加新的命令类型
- **扩展模型**：替换或添加新的 ONNX 模型
- **多语言支持**：添加其他语言的模型和配置
- **传感器集成**：集成跌倒检测、GPS 定位等功能

---

## 附录：文件清单汇总

### 前端文件（src/）

| 目录 | 文件数 | 说明 |
|---|---|---|
| `views/` | 10 | 页面组件 |
| `components/` | 2 | 通用组件 |
| `router/` | 1 | 路由配置 |
| `utils/` | 1 | 语音交互服务 |
| `capacitor/plugins/` | 2 | Capacitor 插件桥接 |
| `assets/` | 1 | 静态资源 |
| 根目录 | 3 | App.vue, main.js, style.css |

### Android 文件（android/）

| 目录 | 文件数 | 说明 |
|---|---|---|
| `java/.../plugins/` | 2 | Capacitor 插件 |
| `java/.../` | 4 | MainActivity + Kotlin 引擎 |
| `assets/` | 3 | 模型文件 |
| `res/` | 多个 | 资源文件 |
